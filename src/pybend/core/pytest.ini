[pytest]
testpaths = tests
pythonpath = .
addopts = --import-mode=importlib --tb=short
markers =
    integration: Integration tests requiring full app + DB
    unit: Unit tests with mocks, no DB required
